{
  "name": "claude-usage-bridge",
  "version": "1.0.0",
  "description": "Local Claude OAuth usage bridge for Cardputer",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "check": "node --check index.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "pino-pretty": "^13.1.3"
  }
}
